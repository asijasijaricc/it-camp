import React from "react";
import ReactDOM from "react-dom/client";
import Class18 from "./components/18. cas";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Class18 />
  </React.StrictMode>
);
